#ifndef __WORD_BUFF_H 
  #define __WORD_BUFF_H 1
#endif

/* Node stuct to to be used in btree*/
// typedef struct Node;

/* addw: puts a word in btree in corret order */
void addw(char *);

/* printw: prints btree in correct order */
void printw();
